# RP8086

**Аппаратно-программный комплекс на базе Raspberry Pi Pico (RP2040) в качестве чипсета для процессора Intel 8086**

RP2040 выполняет роль контроллера шины, эмулятора ROM/RAM и набора микросхем IBM PC (8259A, 8253, 8237, 8272A), обеспечивая полнофункциональную работу i8086 на частоте 3.5 МГц.

## ✨ Основные возможности

- 🚀 **Стабильная работа на 3.5 МГц** — полная эмуляция шины i8086 с аппаратным ускорением через PIO
- 💾 **192 КБ RAM + 8 КБ ROM** — Turbo XT BIOS v3.1, эмуляция MDA видеопамяти (4 КБ)
- 🎮 **Полная эмуляция IBM PC chipset**:
  - Intel 8259A (Programmable Interrupt Controller)
  - Intel 8253 (Programmable Interval Timer, 18.2 Hz)
  - Intel 8237 (DMA Controller, 4 канала)
  - Intel 8272A (Floppy Disk Controller)
- 💿 **Загрузка DR-DOS 7** с эмулированного дискового образа
- ⚡ **Двухъядерная архитектура** — Core0 (UI/клавиатура), Core1 (шина/IRQ)
- 📺 **Вывод видео через USB** — рендеринг MDA текста 25×80 @ 60 FPS в терминал

## 🏗️ Архитектура

### Двухслойная обработка шины

```
┌─────────────────────────────────────────────────────────┐
│  Intel 8086 CPU @ 3.5 MHz                               │
└──────────────────────┬──────────────────────────────────┘
                       │ 20-bit адрес + управляющие сигналы
                       ↓
┌─────────────────────────────────────────────────────────┐
│  Слой 1: PIO State Machine @ 400 MHz (аппаратный)      │
│  • Захват адреса/данных                                 │
│  • Управление READY (wait states)                       │
│  • Генерация IRQ0/IRQ1/IRQ3                             │
└──────────────────────┬──────────────────────────────────┘
                       │ FIFO + IRQ
                       ↓
┌─────────────────────────────────────────────────────────┐
│  Слой 2: ARM Cortex-M0+ @ 400 MHz (программный)        │
│  • Обработка прерываний PIO                             │
│  • Эмуляция RAM/ROM/VRAM                                │
│  • Эмуляция устройств (8259A/8253/8237/8272A)          │
└─────────────────────────────────────────────────────────┘
```

### Разделение задач между ядрами

| Core0 (Main)          | Core1 (Bus Handler)        |
|-----------------------|----------------------------|
| Рендеринг видео 60 FPS | Инициализация PIO         |
| Обработка клавиатуры  | Обработка bus IRQ         |
| USB serial I/O        | Генерация Timer IRQ (18.2 Hz) |
| Debug команды         | Управление сигналом INTR  |

## 📌 Распиновка GPIO

### Шина данных/адреса (26 GPIO)

| GPIO      | Назначение | Направление | Описание |
|-----------|------------|-------------|----------|
| 0-15      | AD0-AD15   | Bidirectional | Мультиплексированная шина адрес/данные |
| 16-19     | A16-A19    | Input | Старшие биты адреса (20-bit адресация) |

### Управляющие сигналы

| GPIO | Сигнал | Направление | Описание |
|------|--------|-------------|----------|
| 20   | ALE    | Input  | Address Latch Enable |
| 21   | RD     | Input  | Read strobe (активный LOW) |
| 22   | WR     | Input  | Write strobe (активный LOW) |
| 23   | INTA   | Input  | Interrupt Acknowledge (активный LOW) |
| 24   | M/IO   | Input  | Memory/IO select (1=память, 0=I/O) |
| 25   | BHE    | Input  | Bus High Enable (8/16-bit операции) |
| 26   | INTR   | Output | Interrupt Request (активный HIGH) |
| 27   | READY  | Output | Wait state control (PIO sideset) |
| 28   | RESET  | Output | Reset (активный LOW) |
| 29   | CLK    | Output | Clock @ 3.5 MHz (PWM, 33% duty cycle) |

> ⚠️ **Важно**: RP2040 является 5V tolerant на входах, поэтому подключение к i8086 (5V логика) возможно напрямую без согласования уровней.

## 🔄 Логика обработки шины

### Цикл чтения (Read Cycle)

```
┌────────┐                                 ┌────────┐
│ i8086  │                                 │ RP2040 │
└───┬────┘                                 └───┬────┘
    │                                          │
    │ 1. ALE=HIGH (адрес валиден)              │
    ├─────────────────────────────────────────>│
    │                                          │ PIO: Захват 26 GPIO
    │ 2. ALE=LOW                               │      Установка READY=0
    ├─────────────────────────────────────────>│      Push в FIFO
    │                                          │      Генерация IRQ1
    │ 3. READY=0 (wait state)                  │
    │<─────────────────────────────────────────┤
    │                                          │
    │                                          │ ARM: Чтение адреса из FIFO
    │                                          │      Доступ к RAM/ROM/Port
    │                                          │      Возврат [data:16][mask:16]
    │                                          │
    │ 4. Данные на AD0-AD15                    │ PIO: Вывод данных
    │<─────────────────────────────────────────┤      Установка READY=1
    │                                          │
    │ 5. RD=HIGH (завершение)                  │
    ├─────────────────────────────────────────>│ PIO: Возврат AD в Hi-Z
    │                                          │
```

**Время обработки**: ~75 тактов RP2040 на цикл (187.5 нс @ 400 МГц)

### Цикл записи (Write Cycle)

```
1. PIO захватывает адрес на ALE
2. PIO захватывает данные с AD0-AD15
3. PIO генерирует IRQ0 на активном WR
4. ARM обрабатывает запись в RAM/Port
5. PIO завершает цикл
```

### INTA протокол (Interrupt Acknowledge)

```
1. Core1 устанавливает INTR=HIGH (при наличии IRQ в 8259A)
2. i8086 инициирует INTA цикл (INTA=LOW)
3. PIO обнаруживает INTA, генерирует IRQ3
4. ARM вызывает i8259_nextirq() → возвращает вектор прерывания
5. Следующий цикл чтения возвращает вектор в формате 0xFF00 | vector
```

## 🛠️ Сборка

### Требования

- [Pico SDK](https://github.com/raspberrypi/pico-sdk) (установить `PICO_SDK_PATH`)
- CMake ≥ 3.13
- ARM GCC toolchain
- Git

### Команды сборки

```bash
cd cmake-build-release
cmake --build .

# Результат: bin/rp2040/Release/RP8086.uf2
```

### Прошивка

1. Удерживая кнопку BOOTSEL, подключите Pico к USB
2. Скопируйте `RP8086.uf2` на появившийся диск `RPI-RP2`
3. Pico автоматически перезагрузится с новой прошивкой

## 🎯 Использование

### Подключение к терминалу

```bash
# Linux/macOS
screen /dev/ttyACM0 115200

# Windows (PuTTY, RealTerm и т.д.)
COM порт, 115200 baud
```

### Debug команды

Специальные команды (заглавные буквы, не отправляются в i8086):

- **M** — Memory dump (интерактивный ввод адреса в hex)
- **V** — Video RAM dump (первые 5 строк × 80 символов)
- **R** — Reset CPU (сброс i8086)
- **B** — Reboot to bootloader (перепрошивка RP2040)

Все остальные символы автоматически конвертируются в IBM PC/XT скан-коды и отправляются в i8086.

## 📊 Технические характеристики

| Параметр | Значение |
|----------|----------|
| **Частота RP2040** | 400 МГц (разгон с повышением напряжения) |
| **Частота i8086** | 3.5 МГц (стабильная работа) |
| **Частота PIO** | 400 МГц (clkdiv = 1.0, без делителя) |
| **RAM** | 192 КБ (0x00000-0x2FFFF) |
| **ROM** | 8 КБ Turbo XT BIOS v3.1 (0xFE000-0xFFFFF) |
| **Video RAM** | 4 КБ MDA (0xB0000-0xB0FFF) |
| **Время обработки шины** | ~75 тактов RP2040 на цикл |
| **Загрузка Core1** | ~45% (worst-case при постоянном доступе к шине) |
| **Поддержка дисков** | 160 КБ - 1.44 МБ (через 8272A FDC) |

## 📚 Документация

- **[CLAUDE.md](CLAUDE.md)** — подробная документация по архитектуре, разработке и оптимизации
- **[docs/specs/](docs/specs/)** — Машиночитаемые спецификации аппаратных компонентов IBM PC в формате TOON, используемые для детального описания эмулируемых устройств. Включают:
  - `Intel 8086` (Процессор)
  - `8259A PIC` (Контроллер прерываний)
  - `8253 PIT` (Таймер)
  - `8237A DMA` (Контроллер прямого доступа к памяти)
  - `8272A FDC` (Контроллер дисковода)
  - `8042` (Контроллер клавиатуры)
  - `MC146818 RTC` (Часы реального времени)
  - `Motorola 6845 CRTC` (Видеоконтроллер)
  - `PC16550D UART` (Последовательный порт)
- **[docs/datasheets/](docs/datasheets/)** — оригинальные даташиты Intel

## 🏆 Достижения

✅ Успешно загружается DR-DOS 7 с эмулированного дискового образа
✅ Полная эмуляция IBM PC chipset (8259A/8253/8237/8272A)
✅ Стабильная работа на 3.5 МГц (увеличение с 5 КГц в 700 раз)
✅ Нулевые wait states в оптимальном случае
✅ Поддержка прерываний (IRQ0 timer, IRQ1 keyboard, IRQ6 floppy)

## 📝 Лицензия

*TODO: Добавить лицензию*

---

**Разработано с использованием C23, Pico SDK и PIO assembly**
